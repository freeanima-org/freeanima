---
---

<script
  define:vars={{
    mermaidDialog: "Mermaid 图表",
    mermaidZoomIn: "放大",
    mermaidZoomOut: "缩小",
    mermaidZoomReset: "重置缩放",
    mermaidClose: "关闭",
    mermaidExpand: "展开图表",
  }}
>
  import { initMermaidClient } from "../lib/mermaid-client.ts";

  initMermaidClient({
    dialog: mermaidDialog,
    zoomIn: mermaidZoomIn,
    zoomOut: mermaidZoomOut,
    zoomReset: mermaidZoomReset,
    close: mermaidClose,
    expand: mermaidExpand,
  });
</script>
