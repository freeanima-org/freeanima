---
description: 仓库拓扑、包命名 RFC、tsconfig paths、oxlint 护栏
globs: packages/**/*,**/tsconfig*.json,**/package.json
alwaysApply: false
---

# Repository topology & packages

> **Code is SSOT** — 结构漂移时与代码同 PR 改本文件。分层矩阵 → [`code-layers.mdc`](code-layers.mdc)。

## Packages

| Package | Path | Role |
| ------- | ---- | ---- |
| `freeanima` | 仓库根 | 编排（just / scripts / tests / docs）；workspace 根 |
| `@freeanima/shared` | `packages/shared/` | 前后端共享契约与纯工具（不发布） |
| `@freeanima/habitat` | `packages/habitat/` | 栖息地进程栈 + CLI + MCP + features 服务端（不发布） |
| `@freeanima/frontend` | `packages/frontend/` | client / ui-kit / features UI / Web·扩展·Tauri（不发布） |
| `@freeanima/site` | `site/` | Astro/Starlight 文档站 |

分层方向 SSOT：[`docs/product/src-layering.md`](../../docs/product/src-layering.md)。

## Import prefixes（RFC #1）

| Shape | Pattern | Example |
| ----- | ------- | ------- |
| Habitat layer | `@freeanima/habitat/{layer}` | `habitat/kernel` … `habitat/platform` |
| Capability | `@freeanima/habitat/capabilities/{slug}` | `self`, `memory`, `tools`, `outpost` |
| Feature | `@freeanima/features/{slug}/…` | 解析到 `packages/{frontend,habitat}/features/…` |
| Shared | `@freeanima/shared/{name}` | `habitat-rpc`, `rpc-contract`, `companion-app` |
| UI kit | `@freeanima/ui-kit` | 设计系统 |
| Client | `@freeanima/client/{portal-sdk,app-frame}` | Portal chrome |
| Entry | `@freeanima/portal/{cli,app,extension}` | CLI ∈ habitat；app/extension ∈ frontend |

**Deprecated**：`@freeanima/host/*`、`@freeanima/frontend/*`（旧逻辑前缀）、`@freeanima/runtime`、裸 `@freeanima/platform`、`@freeanima/core`。

## `packages/` map

```
packages/
├── shared/                 # 无 React
├── habitat/
│   ├── {kernel,core,engine,capabilities,platform}/
│   ├── portal/cli/
│   └── features/<slug>/{domain,habitat,protocol,plugin.ts,…}
└── frontend/
    ├── client/{portal-sdk,app-frame}
    ├── ui-kit/
    ├── portal/{app/{tauri,web},extension}
    └── features/<slug>/{ui,lib,…}
```

MCP 在 `packages/habitat/capabilities/mcp-server`。Habitat 管理台 = `features/habitat`（UI 在 frontend，RPC 在 habitat）。

## Module resolution

- TS：`tsconfig.base.json` paths（shared / habitat / client / ui-kit / 双包 features·portal）
- Vite：`packages/frontend/client/app-frame/vite/module-aliases.ts` + `freeanimaResolvePlugin`

## 护栏

- 根 `.oxlintrc.json` SSOT + `overrides` 按 `packages/frontend/**` 目录覆写；跨层靠 `layer-deps`
- oxlint `freeanima/import-depth` / `layer-deps`；`dir-import/dir-import-exists`（`bun-plugin-dir-import`）
- `just qa check-package-deps`
- 文档站：`cd site && bun run build`（先根 `bun install`）
