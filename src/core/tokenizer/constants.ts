/** Legacy Hub fallback repo id (unit tests / resolve candidates only; not loaded in-process). */
export const FALLBACK_TOKENIZER_REPO = "FacebookAI/xlm-roberta-base";

/** In-process token count backend: heuristic estimate via `tokenx` (no full vocab). */
export const TOKENX_ESTIMATE_REPO = "__estimate__:tokenx";

export const HF_HUB_BASE = "https://huggingface.co";
