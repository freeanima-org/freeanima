{
  "extends": "../../tsconfig.composite.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "outDir": ".tsout"
  },
  "references": [
    { "path": "../../packages/shell-sdk" },
    { "path": "../../packages/shell-ui/vite" },
    { "path": "../../satellites/companion" }
  ],
  "include": [
    "app/**/*.ts",
    "bootstrap/**/*.ts",
    "electron/**/*.ts",
    "src/**/*.ts",
    "dev-electron.ts",
    "build-electron.ts",
    "vite.config.ts",
    "../../types/**/*.d.ts"
  ],
  "exclude": ["**/*.test.ts", "dist/**", "electron-dist/**", "release/**"]
}
