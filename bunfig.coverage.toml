# CI / coverage:cobertura 用：无 coverageThreshold，阈值由 scripts/check-coverage-threshold.ts 校验
[test]
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"

coveragePathIgnorePatterns = [
  "**/node_modules/**",
  "**/*.test.ts",
  "**/*.spec.ts",
  "**/tests/helpers/**",
]
