/** Web shell composition root — settings bindings by runtime kind. */
export {
  detectShellRuntimeKind,
  getShellKind,
  resolveShellBindings,
  type ShellRuntimeKind,
} from "../lib/shell-composition.ts";
