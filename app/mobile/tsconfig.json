{
  "extends": "../../tsconfig.composite.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "outDir": ".tsout"
  },
  "references": [
    { "path": "../../frontend/shell-sdk" },
    { "path": "../../frontend/shell-ui/vite" }
  ],
  "include": [
    "app/**/*.ts",
    "bootstrap/**/*.ts",
    "src/**/*.ts",
    "vite.config.ts",
    "../../types/**/*.d.ts"
  ],
  "exclude": ["**/*.test.ts", "dist/**"]
}
