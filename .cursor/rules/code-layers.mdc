---
description: 13 包单向 DAG、composition root、禁止跨包 import、护栏
globs: packages/**/*
alwaysApply: false
---

# Code layers

> Repository 分层（≠ 认知层）。**代码 + oxlint `freeanima/layer-deps` + `scripts/check-layer-deps.ts` + `package.json`** 四重约束。
> 目录地图 / 前缀 → [`repository-topology.mdc`](repository-topology.mdc)；理由与债务 → [`docs/product/src-layering.md`](../../docs/product/src-layering.md)。

## 服务端链（高 → 低）

```
cli / server → features → capabilities → engine → core → kernel → shared
```

| Layer | Directory | Responsibility |
| ----- | --------- | -------------- |
| kernel | `packages/kernel/` | Cordis 组合根句柄（`createKernel` / `getRootContext`）、logger、**config-mechanism**、logging |
| core | `packages/core/` | drizzle schema/migrations、PG、Redis、产品 config 段 + runtime store、tool/LLM 原语、tokenizer/tts/compress |
| engine | `packages/engine/` | conversation、turn、goal、pipeline、**loop-mechanism**（依赖 core，故不在 kernel） |
| capabilities | `packages/capabilities/` | memory、tools、connectors、self、outpost、mcp-*、federation、llm-openai |
| features | `packages/features/<slug>/` | 服务端特性：`cordis-plugin.ts` + `domain/` + `habitat/` + `protocol/` |
| server | `packages/server/` | 组合根：boot、habitat-api、`ports/`、`config/` 真实现、tls |
| cli | `packages/cli/` | `anima` / `anima-client` / `anima-probe` |

`server` 与 `cli` 是入口/组合根，可依赖其下全部服务端包。

## 前端链（高 → 低）

```
portal → app-frame → ui-features → { portal-sdk, ui-kit } → shared
```

- Feature UI **禁止** import app-frame / 任何服务端包 / `features/*/domain`
- UI 特性入口是 `ui-features/<slug>/ui/<bundle>/index.ts`（公共面）；跨特性只经该入口
- Typed Habitat client：`@freeanima/portal-sdk/habitat-typed-client`
- method defs SSOT：`@freeanima/shared/rpc-contract/feature-rpc`（服务端 routes 与浏览器 registry 共用）
- Display/snapshot：`@freeanima/shared/rpc-contract/frames/{display,snapshot}`

## Dependency matrix

| Source | Allowed | Forbidden |
| ------ | ------- | --------- |
| `shared` | ——（叶层） | 任何其它 `@freeanima` 包、React、drizzle |
| `kernel` | shared | core+、React |
| `core` | kernel、shared | engine+、React |
| `engine` | core、kernel、shared | capabilities+、React |
| `capabilities/*` | engine、core、kernel、shared、自身 | server、features、其它 capability |
| `features/*` | capabilities、engine、core、kernel、shared | server、portal-sdk、ui-* |
| `server` | features、capabilities、engine、core、kernel、shared | ui-kit、portal-sdk、ui-features、app-frame、portal |
| `cli` | server + 全部服务端包、shared | 前端包 |
| `ui-kit` | shared | portal-sdk+（除 2 处已登记债务）、服务端包 |
| `portal-sdk` | ui-kit、shared | ui-features、app-frame、portal、服务端包 |
| `ui-features` | portal-sdk、ui-kit、shared | app-frame、portal、服务端包 |
| `app-frame` | ui-features、portal-sdk、ui-kit、shared | portal、服务端包 |
| `portal` | app-frame、ui-features、portal-sdk、ui-kit、shared | 服务端包 |

**方向：** `omitUndefined` / CST 工具 SSOT 在 `@freeanima/shared/util`；存储形状吃
`@freeanima/shared/pg-shapes`；PG 只经 `@freeanima/core/db`。

## Cordis 约定

- 进程唯一根 `Context` 由 `createKernel()` 安装；模块级全局桥为 0（oxlint
  `no-module-globals` + `scripts/check-module-globals.ts`）。
- 服务：`class X extends Service` + `declare module "cordis"` 增强 + 幂等 `mountXService(ctx)`。
- 特性：`createFeaturePlugin({ id, routes })`，`inject: ["features"]`，经 `ctx.effect` 注册/撤销。
- 组合根→能力/特性的回调走 `ctx.platformPorts`（`packages/capabilities/ports/service.ts`）。

## CI

`just qa check` → tsgo + oxlint（含 `layer-deps` / `no-module-globals`）+ stylelint +
oxfmt + 变更测试 + `check-{frontend-no-drizzle,shared-shapes,package-deps,layer-deps,module-globals,boot-plugin-parity}` +
`codemod-freeanima-imports --check`。
