---
description: UI 三维度正交（壳子/布局/交互）— 改前端前必读
globs: src/frontend/**/*,src/features/**/ui/**/*,src/app/shell/**/*,src/satellites/**/spa/**/*
alwaysApply: false
---

# UI 三维度

编辑匹配文件前，打开并遵循 [`.agent/rules/ui-dimensions.md`](../../.agent/rules/ui-dimensions.md)。

硬禁止：

1. 禁止 `isMobile = getShellKind() === "tauri"` 或用壳类型锁主布局
2. 禁止单个 `isMobile`/`isDesktop` 同时管布局与交互
3. 禁止用视口断点决定右键/长按/Enter 发送

选用 API：

- 壳：`getShellKind` / `canOpenHabitatSettings` / `shouldUseNativeShellNavigation`
- 布局：`useLayoutMode` / `useCompactLayout` / `useDrawerNav`
- 交互：`useContextMenuCapability` / `useActionSheetCapability` / `useEnterToSendCapability`
